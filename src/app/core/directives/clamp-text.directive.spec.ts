import { ClampTextDirective } from './clamp-text.directive';

describe('ClampTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ClampTextDirective();
    expect(directive).toBeTruthy();
  });
});
